import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
    alignItems: 'center',
  },
  containerList: {
    // marginLeft: 20,
    paddingLeft: 20,
    marginTop: 40,
  },
  contentList: {},
  imageBottom: {},
})
